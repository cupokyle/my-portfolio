import '../App.scss';
import React from "react";

export default function Hiring() {
    return <section className="resumeBox">
        <p>This is really all a portfolio needs to be</p>
    </section>
}